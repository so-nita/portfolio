import React, { useEffect, useRef } from 'react';
import * as THREE from 'three';
import HALO from 'vanta/dist/vanta.halo.min';

const VantaBackground = ({ children }) => {
    const vantaRef = useRef(null);
    
};


exports = VantaBackground;